import players from '../players.json'

const initialState = {
  players
}

export const playersReducer = (state=initialState, action) => {
  switch(action.type) {
    
    default: 
      return state
  }
}
